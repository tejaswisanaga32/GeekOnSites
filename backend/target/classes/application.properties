spring.application.name=call-management

# MongoDB Configuration - Cloud Atlas (Production)
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://tejaswisanaga12_db_user:tejureddy12@cluster0.q9mxdq2.mongodb.net/geekonsite_calls?retryWrites=true&w=majority}

# Server Configuration
server.port=${PORT:8080}

# JWT Configuration
jwt.secret=${JWT_SECRET:geekonsiteSecretKey2024ForCallManagementSystem}
jwt.expiration=${JWT_EXPIRATION:86400000}

# CORS Configuration - Allow all for production
cors.allowed-origins=*

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
